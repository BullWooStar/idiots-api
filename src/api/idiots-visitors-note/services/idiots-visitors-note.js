'use strict';

/**
 * idiots-visitors-note service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::idiots-visitors-note.idiots-visitors-note');
