'use strict';

/**
 * idiots-visitors-note controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::idiots-visitors-note.idiots-visitors-note');
