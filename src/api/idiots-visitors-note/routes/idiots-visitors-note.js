'use strict';

/**
 * idiots-visitors-note router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::idiots-visitors-note.idiots-visitors-note');
